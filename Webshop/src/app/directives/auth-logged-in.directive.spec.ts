import { AuthLoggedInDirective } from './auth-logged-in.directive';

describe('AuthLoggedInDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthLoggedInDirective();
    expect(directive).toBeTruthy();
  });
});
