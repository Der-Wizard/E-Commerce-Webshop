import { StockEvaluationDirective } from './stock-evaluation.directive';

describe('StockEvaluationDirective', () => {
  it('should create an instance', () => {
    const directive = new StockEvaluationDirective();
    expect(directive).toBeTruthy();
  });
});
